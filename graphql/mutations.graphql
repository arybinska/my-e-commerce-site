mutation CreateProductReview($review: ReviewCreateInput! ) {
  review: createReview(data: $review) {
    id
    stage
  }
}

mutation CreateCheckoutOrder($order: CheckoutCreateInput! ) {
    order: createCheckout(data:$order) {
    id
  }
}